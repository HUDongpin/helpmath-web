# Release record — PR #9 release assurance and legacy cutover hardening

## Identity

| Field | Evidence |
| --- | --- |
| Candidate commit | `4bc0c498f898ce6c49de58383ca2fccf2a5b0d7d` |
| Pull request | [#9](https://github.com/HUDongpin/helpmath-web/pull/9), merged with squash on `2026-07-21T11:28:39Z` |
| Production commit | `ed1bad4cd5f84759cf816995fab00d002abf85ce` |
| Candidate Quality run | [29824444462](https://github.com/HUDongpin/helpmath-web/actions/runs/29824444462), `success`, matching the candidate commit |
| Production Quality run | [29826254041](https://github.com/HUDongpin/helpmath-web/actions/runs/29826254041), `success`, matching the production commit |
| Protected Preview | GitHub deployment `5537067009`, Vercel deployment [E3QZJ4iMS74pMwrifgGL3dDpBV39](https://vercel.com/peter-dongpin-hu-s-projects/helpmath-web/E3QZJ4iMS74pMwrifgGL3dDpBV39), `success`, matching the candidate commit; protected URL `https://helpmath-qdh6qg5x0-peter-dongpin-hu-s-projects.vercel.app` |
| Production | GitHub deployment `5537444603`, Vercel deployment [G8KErBhE2BdgwQtPhEoSJsSRYWZG](https://vercel.com/peter-dongpin-hu-s-projects/helpmath-web/G8KErBhE2BdgwQtPhEoSJsSRYWZG), `success`, matching the production commit; immutable URL `https://helpmath-l1do9dj1z-peter-dongpin-hu-s-projects.vercel.app` |
| Contact mode | Disabled; the page renders no intake form and `/api/contact` fails closed |

The repository and Preview are private/protected. This operational record is
not authorization to make either JavaScript demo public, publish an
owner-held source document, activate contact intake, or change the historical
host or its DNS.

## Verified release gates

The candidate Quality run passed generated-file verification, ESLint,
TypeScript, unit and contract tests, the private-runtime build and leak checks,
the Next.js production build, Playwright, and the high-severity dependency
audit. Its browser matrix completed `55/55` tests across all 26 application
routes at desktop and mobile viewports. The production commit reran the same
Quality workflow successfully, with every recorded job step completing.

The Vercel Preview deployment was tied to the candidate commit and completed
successfully. The retained post-deployment probe reached the Vercel SSO
boundary and confirmed a protected `302`, `Cache-Control: no-store`,
`X-Robots-Tag: noindex`, HSTS, and `X-Frame-Options: DENY`. The operator's
resolver was inconsistent during that probe, so a public resolver and a
diagnostic-only `curl --resolve` to one returned Vercel edge were used. No DNS
or deployment setting was changed. Because no Vercel Automation Bypass secret
was used, this proves the Preview protection boundary but is not a semantic
smoke of pages behind that boundary.

Production contract verification was repeated against
`https://www.helpmath.ai` after promotion and completed with no failures. The
following output was observed immediately after the command completed at
`2026-07-21T11:56:40Z`; no executive access key was supplied for this repeat,
so authenticated demo counts are intentionally zero while all unauthenticated
privacy boundaries remain covered:

```json
{
  "baseUrl": "https://www.helpmath.ai",
  "canonicalOrigin": "https://www.helpmath.ai",
  "fetchTimeoutMs": 20000,
  "internalLinkConcurrency": 6,
  "requestMaxAttempts": 3,
  "requestRetryBaseDelayMs": 300,
  "requestRetryMaxDelayMs": 2000,
  "expectedSitemapPages": 20,
  "publicPages": 20,
  "internalLinks": 52,
  "englishPrefixRedirects": 3,
  "legacyRedirects": 11,
  "brandedNotFoundCases": 7,
  "privateDemoRoutes": 4,
  "closedLegacyDemoAssets": 12,
  "executivePreviewAssets": 12,
  "privateDemoOptimizerProbes": 4,
  "executivePreviewEntries": 2,
  "executivePreviewAuthentication": "not-requested",
  "executivePreviewAuthenticatedDemoRoutes": 0,
  "executivePreviewAuthenticatedAssets": 0,
  "executivePreviewAuthenticatedRuntimes": 0,
  "staticAssets": 4,
  "legalDrafts": 4,
  "domainMatrixCases": 8,
  "contactExpectation": "disabled",
  "protectedPreviewAuth": false,
  "failures": []
}
```

## Release scope and boundaries

- The release expanded the browser gate to every public, legal-draft, and
  executive-entry route at desktop and mobile sizes, including accessibility,
  console, link, and overflow checks.
- Both `/Beta/:path*` and `/beta/:path*` now resolve through the governed
  curriculum redirect.
- The generated Apache 2.4 cutover package passed its generator tests and real
  local Apache 2.4.66 contract checks in both deployment forms. The release
  evidence repeat completed `8/8` tests before `2026-07-21T12:04:46Z`. It was
  not installed on the historical host, and the actual host's `httpd -t`
  remains a separate cutover gate.
- The finite legacy evidence registry captured 23 of 23 governed HTTPS sources
  as 23 content-addressed objects totaling 14,280,549 bytes. The website
  repository retains metadata only; the original bytes remain in the
  restricted local sibling archive.
- Both JavaScript demo families remained private and non-indexable. Their
  unauthenticated routes, original asset paths, optimizer probes, executive
  assets, and executive runtimes remained closed.
- Privacy and Terms remained visible drafts outside the sitemap. Contact
  delivery remained disabled. The old `helpprogram.net` host and DNS remained
  unchanged.

## Exceptions and follow-up gates

| Gap or exception | Owner | Review date |
| --- | --- | --- |
| A post-deployment semantic smoke behind the protected PR #9 Preview was not retained; the recorded probe proves only the SSO boundary. | Pending release-owner decision in `LAUNCH_DECISIONS.md` | Resolve for a future production candidate by using the protected Automation Bypass workflow. No retrospective pass is asserted. |
| The successful Production deployment event and the zero-failure canonical smoke are retained, but no authenticated Vercel alias-assignment export tying `www.helpmath.ai` to that exact deployment was retained in this record. The two facts must not be combined to infer alias identity. | Pending release owner | Verify in authenticated Vercel evidence for the next release and retain the assignment with the deployment record. |
| The fresh production repeat above did not request executive authentication. Earlier credentialed production checks are summarized in `LAUNCH_DECISIONS.md`, but their raw timestamped JSON was not added to this immutable record. | Pending release owner | Repeat before any extension or public promotion of the executive demos. |
| Durable off-device custody and an independent restore of the restricted legacy-source archive remain pending. | Pending custody owner | Complete before the historical-domain cutover. |
| Legal identity, final legal copy, contact delivery, demo publication rights, release/rollback ownership, DNS/mail cutover, and final authorization remain pending. | Named owners not yet recorded | Required before the corresponding gate is opened. |

These exceptions prevent describing the overall website migration or the
historical-domain cutover as complete. They do not alter the exact commit,
Quality, deployment, public production-smoke, or holding-state facts retained
above.
