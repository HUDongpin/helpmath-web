# Release record — PR #8 historical curriculum and resource library

## Identity

| Field | Evidence |
| --- | --- |
| Candidate commit | `153ec6fd800addfb38a493967154eb1bc9c2fe5f` |
| Pull request | [#8](https://github.com/HUDongpin/helpmath-web/pull/8), merged with squash on `2026-07-21T09:02:33Z` |
| Production commit | `ff3176a0c4b98874613e759df738f27b4b04ce84` |
| Candidate Quality run | [29815855145](https://github.com/HUDongpin/helpmath-web/actions/runs/29815855145), `success`, matching the candidate commit |
| Production Quality run | [29816653924](https://github.com/HUDongpin/helpmath-web/actions/runs/29816653924), `success`, matching the production commit |
| Protected Preview | `dpl_GwW5h4BvJpjxbgYnVXyR8tAXQnVm`, `READY`, matching the candidate commit |
| Production | `dpl_5z1uh1S5aURhcB6X2Yt66BVcoNQz`, `READY`, assigned to `www.helpmath.ai`, `helpmath.ai`, and the project production aliases |
| Contact mode | Disabled; the page renders no intake form and `/api/contact` fails closed |

The repository and Preview are private/protected. This operational record is
not authorization to make either JavaScript demo public or to republish an
owner-held historical document.

## Verified release gates

The candidate passed generated-file verification, ESLint, TypeScript, unit
tests, the private-runtime build and leak checks, the Next.js production build,
Playwright, and the high-severity dependency audit. The candidate browser run
completed `35/35` tests. The production push reran the same GitHub Quality
workflow successfully.

The protected Preview deployment reached `READY`, but no post-deployment
Preview semantic-smoke timestamp, parameters, or zero-failure output was
retained for PR #8. That is a historical evidence gap, not an inferred pass.

Production contract verification returned the following zero-failure summary
on 2026-07-21 after Vercel assigned the canonical aliases:

```json
{
  "baseUrl": "https://www.helpmath.ai",
  "canonicalOrigin": "https://www.helpmath.ai",
  "fetchTimeoutMs": 20000,
  "internalLinkConcurrency": 6,
  "requestMaxAttempts": 3,
  "requestRetryBaseDelayMs": 300,
  "requestRetryMaxDelayMs": 2000,
  "expectedSitemapPages": 20,
  "publicPages": 20,
  "internalLinks": 52,
  "englishPrefixRedirects": 3,
  "legacyRedirects": 11,
  "brandedNotFoundCases": 7,
  "privateDemoRoutes": 4,
  "closedLegacyDemoAssets": 12,
  "executivePreviewAssets": 12,
  "privateDemoOptimizerProbes": 4,
  "executivePreviewEntries": 2,
  "executivePreviewAuthentication": "not-requested",
  "executivePreviewAuthenticatedDemoRoutes": 0,
  "executivePreviewAuthenticatedAssets": 0,
  "executivePreviewAuthenticatedRuntimes": 0,
  "staticAssets": 4,
  "legalDrafts": 4,
  "domainMatrixCases": 8,
  "contactExpectation": "disabled",
  "protectedPreviewAuth": false,
  "failures": []
}
```

The operator network intermittently reset TLS connections to some Vercel
anycast addresses. For the retained zero-failure run, name lookup was pinned
inside that one process to `216.150.16.1`, which was one of the live DNS A
answers observed during the release window. Separate live DNS queries and
`curl --resolve` probes verified both canonical hosts, HTTP-to-HTTPS behavior,
the apex-to-`www` redirect, and multiple Vercel edge addresses. This diagnostic
pin selected a returned edge; it did not change public DNS or the deployment.

## Publication boundaries and exceptions

- The two demo routes remained unauthenticated `404` responses with
  `noindex, nofollow, noarchive`; their runtime and image derivatives remained
  behind the executive-session boundary.
- Authenticated executive-preview smoke was not repeated for PR #8 because the
  protected runtime was unchanged and the production WAF test budget is
  intentionally bounded. Its prior authenticated verification remains in
  [`LAUNCH_DECISIONS.md`](../LAUNCH_DECISIONS.md).
- Privacy and Terms remained visible drafts, outside the sitemap, with
  `noindex, follow`.
- Contact delivery remained disabled because legal, inbox, Turnstile, Resend,
  retention, access, and real-delivery approvals are still open.
- The old `helpprogram.net` host and DNS were not changed.
- Owner-held PDFs were not published; rights and accessibility review remains
  pending.

## Historical evidence gaps

| Gap or exception | Owner | Review date |
| --- | --- | --- |
| Protected Preview post-deployment semantic-smoke evidence was not retained. | Pending release-owner decision in `LAUNCH_DECISIONS.md` | Must be resolved for the next production candidate; no retrospective date is asserted. |
| Named ownership and dated review for the exceptions above were not recorded during PR #8. | Pending release and rollback owners in `LAUNCH_DECISIONS.md` | Must be assigned before cutover or the next production promotion. |

These gaps prevent describing PR #8 as a complete `RELEASE_EVIDENCE.md`
record. They do not alter the retained CI, deployment-identity, or production
smoke facts above.
