# Release record — PR #32 localized error recovery and keyboard resilience

## Identity

| Field | Evidence |
| --- | --- |
| Candidate commit | `a5f90ddd579af0ca288fd28be21e4621e9fff340` |
| Pull request | [#32](https://github.com/HUDongpin/helpmath-web/pull/32), merged with squash on `2026-07-22T08:47:20Z` |
| Production commit | `f297d0230264577b19f5f06274bd802a6e725b48` |
| Candidate Quality run | [29904847279](https://github.com/HUDongpin/helpmath-web/actions/runs/29904847279), `success`, matching the candidate commit |
| Production Quality run | [29905358787](https://github.com/HUDongpin/helpmath-web/actions/runs/29905358787), `success`, matching the production commit |
| Protected Preview | GitHub deployment `5552353099`, status `15786099265`; Vercel deployment [C9bRUB5658B6sbB5yTL3NtMvYBmR](https://vercel.com/peter-dongpin-hu-s-projects/helpmath-web/C9bRUB5658B6sbB5yTL3NtMvYBmR), `success`, matching the candidate commit; protected URL `https://helpmath-6oulvlnyq-peter-dongpin-hu-s-projects.vercel.app` |
| Production | GitHub deployment `5552454732`, status `15786395484`; Vercel deployment [FirM93JbPzFneEXQX4UWJZdN9XYn](https://vercel.com/peter-dongpin-hu-s-projects/helpmath-web/FirM93JbPzFneEXQX4UWJZdN9XYn), `success`, matching the production commit; immutable URL `https://helpmath-ct3kg6rpm-peter-dongpin-hu-s-projects.vercel.app` |
| Canonical alias assignment | Owner-authenticated Vercel CLI `56.4.1` inspection reported `READY` Production deployment `dpl_FirM93JbPzFneEXQX4UWJZdN9XYn` with both `https://www.helpmath.ai` and `https://helpmath.ai` assigned after the deployment became ready; the reduced non-secret evidence is `docs/evidence/vercel-production-alias-2026-07-22-pr32.json`, SHA-256 `c6b7f270a24b0eee5194975162b816ab9f521d6d4ce6ffbb0196da0d4aa264ba` |
| Contact mode | Disabled; repository gates remain `holding`, the page renders no intake form, and `/api/contact` fails closed |

This private operational record is not authorization to publish either
JavaScript demo, activate contact delivery, publish draft legal copy, change
the historical host or its DNS, or treat an Executive Preview as product or
rights acceptance.

## Verified release gates

The final candidate and Production Quality runs passed generated-file and
launch-gate verification, ESLint, TypeScript, 215 unit and contract tests, 15
release-smoke helper tests, coverage thresholds, the high-severity dependency
audit, private-runtime build and leak checks, the Next.js production build,
the full Chromium contract, focused desktop WebKit, native iPhone WebKit, and
Firefox matrices, seven visual regression baselines in the digest-pinned
Linux container, and five-route Lighthouse budgets.

Candidate run `29904847279` completed 86 browser tests: 85 passed, one was an
intentional skip, and none failed. Its visual artifact `8523482578` has digest
`sha256:1121e0b16eca024fd85894f40ebf55d9ec441f21d623278d22bc8c3f599002cd`
and expires on `2026-08-21T08:43:51Z`; its Lighthouse artifact `8523488851`
has digest
`sha256:7dff146376191900d9eecfd1feee0578b3a2a41bee95b5a73498d916fdd21b43`
and expires on `2026-08-21T08:44:04Z`.

Production run `29905358787` completed the same 86-browser-test matrix: 85
passed, one was an intentional skip, and none failed. Its seven visual
baselines passed, including the 320-pixel Spanish Terms header. The Production
visual artifact `8523693661` has digest
`sha256:8200132d3af432af14ef3a02dd8b5af6645f4938ac5b8b2b038806da392f564b`;
the Production Lighthouse artifact `8523694063` has digest
`sha256:545d790243d5babfb994f0d1afe0bb8d21d6253cfbe4c191d5e0d097d1bd34d3`.
Both expire on `2026-08-21T08:52:02Z`.

The final implementation adds localized route and root-layout recovery pages
for English and Spanish, keeps error messages, stacks, and digests out of the
rendered fallback, and provides retry, home, and support actions with heading
focus. It replaces the native mobile `details` menu with one hydrated,
controlled button-and-panel state. Escape returns focus synchronously, menu
boundary Tab and Shift+Tab move to visible adjacent controls, and the closed
trigger uses the same explicit handoff so WebKit cannot drop focus on the
document body. A no-wait Escape-to-Tab regression is retained in the browser
suite. Focus indicators use an ink outline plus white separation ring and
pass the reviewed palette contrast checks.

Pre-merge pressure checks recorded in PR #32 completed the complete focus flow
80 times across Chromium, Firefox, desktop WebKit, and mobile WebKit. The first
controlled-button candidate exposed one stable 320-pixel visual difference
because the browser default centered the Spanish menu label. The final
candidate explicitly restored left alignment; both candidate and Production
Linux visual gates then passed 7 of 7 without changing an approved baseline
image.

Candidate and Production Vercel logs reported `Vercel build required:
build-relevant-change`, ran `npm ci` with zero vulnerabilities, generated all
38 Next.js route entries, and found zero private-demo static leaks. The
Production build used Vercel build `bld_dd2l5pupi` and completed before the
canonical aliases were assigned.

Automated production-smoke run
[29905407356](https://github.com/HUDongpin/helpmath-web/actions/runs/29905407356)
completed successfully at `2026-07-22T08:49:24.300Z` against the canonical
domain and the immutable deployment protection boundary. Its retained
`production-smoke-29905407356` artifact has GitHub artifact ID `8523624106`,
digest
`sha256:f75447f01ea93e230bc464bf4e208d591032ac5d9206d1dcae63919960b26b3b`,
and expires on `2026-10-20T08:48:09Z`. The retained
`canonical-contract.json` has SHA-256
`57d82fb089dde35bd4cac209ddd1bff6799632ce60c57890b2ed3ce024f22ca8` and
records no failures:

```json
{
  "baseUrl": "https://www.helpmath.ai",
  "canonicalOrigin": "https://www.helpmath.ai",
  "launchGateManifestSha256": "44fb3b69e86bf8e0819f37e168007f44cfe8c4319cf151f8a80ad8cfb94432c4",
  "launchGates": {
    "legalPublication": "holding",
    "contactIntake": "holding",
    "demoPublication": "holding",
    "legacyCutover": "holding",
    "productionLaunch": "holding"
  },
  "fetchTimeoutMs": 20000,
  "internalLinkConcurrency": 6,
  "requestMaxAttempts": 3,
  "requestRetryBaseDelayMs": 300,
  "requestRetryMaxDelayMs": 2000,
  "expectedSitemapPages": 20,
  "publicPages": 20,
  "internalLinks": 54,
  "englishPrefixRedirects": 3,
  "legacyRedirects": 11,
  "brandedNotFoundCases": 7,
  "privateDemoRoutes": 4,
  "privateDemos": 2,
  "publicDemos": 0,
  "indexableDemos": 0,
  "closedLegacyDemoAssets": 12,
  "privateExecutivePreviewAssets": 12,
  "publicExecutivePreviewAssets": 0,
  "executivePreviewRuntimeProbes": 2,
  "demoOptimizerProbes": 6,
  "executivePreviewEntries": 2,
  "executivePreviewExpectedState": "any",
  "executivePreviewExpectedExpiresAt": null,
  "executivePreviewState": "login",
  "executivePreviewExpiresAt": "2026-07-28T15:59:00.000Z",
  "executivePreviewAuthentication": "not-requested",
  "executivePreviewAuthenticatedDemoRoutes": 0,
  "executivePreviewAuthenticatedAssets": 0,
  "executivePreviewAuthenticatedRuntimes": 0,
  "staticAssets": 4,
  "legalPublicationGate": "holding",
  "legalDrafts": 4,
  "legalPublishedPages": 0,
  "domainMatrixCases": 8,
  "contactRepositoryGate": "holding",
  "contactExpectation": "disabled",
  "protectedPreviewAuth": false,
  "failures": []
}
```

The same artifact ties the successful Production deployment-status event to
GitHub deployment `5552454732`, Production commit
`f297d0230264577b19f5f06274bd802a6e725b48`, and the immutable URL above. The
immutable-boundary probe observed Vercel's protected `302`, `no-store`,
`noindex`, HSTS, and frame-denial response. The workflow's public-browser
contract completed five Chromium tests successfully. It did not retain or use
a Vercel bypass or Executive Preview credential.

## Alias-assignment evidence

The owner-authenticated Vercel inspection at `2026-07-22T09:02:57.000Z` tied
both canonical aliases and all project Production aliases to the exact
`READY` deployment named above. Vercel's two Git identity fields both matched
the Production commit. The API recorded deployment readiness at
`2026-07-22T08:48:06.148Z` and alias assignment at
`2026-07-22T08:48:06.985Z`, so the aliases were assigned only after readiness.
It also confirmed build `bld_dd2l5pupi`, repository ignore command
`node scripts/vercel-ignore-build.mjs`, the `build-relevant-change` decision,
and install command `npm ci`. The retained JSON excludes environment data and
unnecessary internal build metadata. It is not a DNS zone export, does not
authorize the old-domain cutover, and does not open any holding launch gate.

## Release scope and boundaries

- The production contract reports two private demos and zero public or
  indexable demos. Direct demo routes and legacy asset paths remain fail-closed
  while `demoPublication` is `holding`.
- The confirmed Boulder Learning–PedaNova strategic partnership remains public
  in both languages. This release does not change that bilateral statement.
- The Executive Preview entry reports the exact approved close and accepts no
  anonymous demo, runtime, or asset access. This release did not run a current
  credentialed playback check.
- Privacy and Terms remain visible drafts outside the sitemap, contact
  delivery remains disabled, and `helpprogram.net` remains unchanged.
- The machine-readable launch manifest keeps all five unresolved gates at
  `holding`; this release does not claim the overall migration or public demo
  launch is complete.

## Exceptions and follow-up gates

| Gap or exception | Owner | Review date |
| --- | --- | --- |
| A semantic smoke behind the protected PR #32 Preview was not retained; Vercel `READY` and commit identity do not prove application behavior behind SSO. | Engineering release owner | Review by `2026-08-05`; resolve for a future candidate only through the private Automation Bypass procedure. No retrospective pass is asserted. |
| The production smoke did not request executive authentication. It proves the entry state, exact close, and unauthenticated privacy boundaries, but not current credentialed playback of either demo. | Executive Preview operator | Review by `2026-07-28T15:59:00Z`, before the approved preview closes; follow `EXECUTIVE_PREVIEW_HANDOFF.md` with the privately held passphrase before the CEO review. |
| The controlled mobile navigation requires JavaScript after server rendering; no no-JavaScript mobile navigation fallback is claimed. | Product/accessibility owner | Review by `2026-08-31`; revisit earlier only if progressive enhancement without JavaScript becomes a product requirement. |
| Durable encrypted off-device custody and an independent restore of the restricted legacy-source archive remain pending. | Legacy archive custody owner | Review by `2026-08-31` and complete before historical-domain cutover. |
| Legal identity, final legal copy, contact delivery, demo publication rights and strict fidelity/product acceptance, executive delivery confirmation, release/rollback ownership, DNS/mail cutover, Search Console, and final authorization remain pending. | Business, legal, product, and release owners for the corresponding gates | Review by `2026-08-31`; every applicable item remains required before its gate is opened. |

These exceptions prevent describing the overall website migration, Executive
Preview handoff, demo publication, or historical-domain cutover as complete.
They do not alter the exact release identity, canonical alias assignment,
Quality, localized runtime recovery, deterministic keyboard navigation,
focus-contrast, visual-regression, WebKit, Firefox, Lighthouse, or zero-failure
production-smoke evidence retained above.
