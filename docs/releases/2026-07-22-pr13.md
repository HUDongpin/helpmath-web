# Release record — PR #13 legacy-cutover preflight and current production evidence

## Identity

| Field | Evidence |
| --- | --- |
| Candidate commit | `2694070a56135e8e0e2e7dac3cffd320beec0507` |
| Pull request | [#13](https://github.com/HUDongpin/helpmath-web/pull/13), merged with squash on `2026-07-21T18:49:39Z` |
| Production commit | `05c3b460db78ac61b88b6d34479d925af634d724` |
| Candidate Quality run | [29858602679](https://github.com/HUDongpin/helpmath-web/actions/runs/29858602679), `success`, matching the candidate commit |
| Production Quality run | [29858878638](https://github.com/HUDongpin/helpmath-web/actions/runs/29858878638), `success`, matching the production commit |
| Protected Preview | GitHub deployment `5543864523`, Vercel deployment [CuCj1zpjrSJ5es7KDSDSwrAxNnb7](https://vercel.com/peter-dongpin-hu-s-projects/helpmath-web/CuCj1zpjrSJ5es7KDSDSwrAxNnb7), `success`, matching the candidate commit; protected URL `https://helpmath-xlh2vywft-peter-dongpin-hu-s-projects.vercel.app` |
| Production | GitHub deployment `5543929832`, `success`, matching the production commit; current owner-observed Vercel deployment [2CCekiWMGdnwc78gdifVSs1NWNsX](https://vercel.com/peter-dongpin-hu-s-projects/helpmath-web/2CCekiWMGdnwc78gdifVSs1NWNsX), `READY`, after the credential-rotation redeploy; immutable URL `https://helpmath-3uwemp4ov-peter-dongpin-hu-s-projects.vercel.app`. Commit association for the current redeploy is the explicit inference described below, not a direct current-deployment Git field. |
| Canonical alias assignment | Owner-authenticated Vercel CLI `56.4.1` inspection reported `READY` Production deployment `dpl_2CCekiWMGdnwc78gdifVSs1NWNsX` with both `https://www.helpmath.ai` and `https://helpmath.ai` assigned; the reduced non-secret evidence is `docs/evidence/vercel-production-alias-2026-07-22.json`, SHA-256 `a60271a276b4301d1877761e455b92a93afc851fe9079a3f207b23645f8eb0f0`, and the contemporaneous record is [PR #13 comment 5037967671](https://github.com/HUDongpin/helpmath-web/pull/13#issuecomment-5037967671) |
| Contact mode | Disabled; repository gates remain `holding`, the page renders no intake form, and `/api/contact` fails closed |

This private operational record is not authorization to publish either
JavaScript demo, activate contact delivery, publish draft legal copy, change
the historical host or its DNS, or treat an executive preview as product,
rights, or strict migration-fidelity acceptance.

## Verified release gates

The candidate and production Quality runs passed generated-file and launch-gate
verification, ESLint, TypeScript, `120` unit and contract tests, `12`
release-smoke helper tests, the private runtime build and leak checks, the
Next.js production build, all `55/55` Playwright checks, and the high-severity
dependency audit. The production build retained `14` private-demo trace files,
`12` private images and `2` runtimes with `0` static leaks.

The same production commit then passed the independently dispatched stable-link
run [29860501241](https://github.com/HUDongpin/helpmath-web/actions/runs/29860501241):
all `16/16` governed external links succeeded and `0` failed. A local operator
probe from another network observed TLS failures for the ERIC/IES hosts at the
same time; the GitHub runner reached those exact registered URLs successfully.
The successful run is the retained same-commit release evidence, while the
regional result remains a reminder that link availability is point-in-time.

The final protected Preview was tied to the candidate commit and reached
Vercel `READY`. No Automation Bypass credential was available for that
deployment, so no semantic assertion is made about application pages behind
its Vercel protection boundary.

## Production smoke and private executive preview

Post-rotation automated production-smoke run
[29859546466](https://github.com/HUDongpin/helpmath-web/actions/runs/29859546466)
completed successfully against the canonical domain and immutable deployment
boundary. Its retained `production-smoke-29859546466` artifact has GitHub
artifact ID `8506703532`, digest
`sha256:dd821aad6775e6583fdf8bd6479b4e0cac6394acb504d3b829468dbadc109f49`,
and expires on `2026-10-19T18:58:59Z`. The canonical JSON records the current
manifest and no failures:

```json
{
  "baseUrl": "https://www.helpmath.ai",
  "canonicalOrigin": "https://www.helpmath.ai",
  "launchGateManifestSha256": "d32e105c55729568f645ac058b55d33e923b76eee893e6e80d61aa907bed1530",
  "launchGates": {
    "legalPublication": "holding",
    "contactIntake": "holding",
    "demoPublication": "holding",
    "legacyCutover": "holding",
    "productionLaunch": "holding"
  },
  "expectedSitemapPages": 20,
  "publicPages": 20,
  "internalLinks": 52,
  "englishPrefixRedirects": 3,
  "legacyRedirects": 11,
  "brandedNotFoundCases": 7,
  "privateDemoRoutes": 4,
  "closedLegacyDemoAssets": 12,
  "executivePreviewAssets": 12,
  "privateDemoOptimizerProbes": 4,
  "executivePreviewEntries": 2,
  "executivePreviewExpectedState": "any",
  "executivePreviewExpectedExpiresAt": null,
  "executivePreviewState": "login",
  "executivePreviewExpiresAt": "2026-07-28T15:59:00.000Z",
  "executivePreviewAuthentication": "not-requested",
  "executivePreviewAuthenticatedDemoRoutes": 0,
  "executivePreviewAuthenticatedAssets": 0,
  "executivePreviewAuthenticatedRuntimes": 0,
  "staticAssets": 4,
  "legalPublicationGate": "holding",
  "legalDrafts": 4,
  "legalPublishedPages": 0,
  "domainMatrixCases": 8,
  "contactRepositoryGate": "holding",
  "contactExpectation": "disabled",
  "protectedPreviewAuth": false,
  "failures": []
}
```

The automated workflow did not request executive authentication because
GitHub Actions deliberately retains no executive credential. A separate local
owner-authenticated operator check against the exact current Production
deployment reported that it:

- reached all `4/4` localized private demo routes (`EN/ES × conversion-1-2/conversion-1-4`);
- reached all `12/12` private images and both `2/2` private runtime bundles;
- observed the exact `2026-07-28T15:59:00.000Z` close and `failures: []`; and
- reported the focused two-demo browser session test `1/1` with a 90-second
  ceiling after a first 30-second run reached the final accessibility scan.

The reduced non-secret operator attestation is
`docs/evidence/executive-preview-production-2026-07-22.json`, SHA-256
`541a6c7cb0da495f18dd271a8fdcd311291f88d8abf3c211050683c9890abc4b`.
No canonical machine result or raw output was retained, so these authenticated
counts are not represented as independently reproducible evidence. Both
sensitive values were rotated together, only the approved local Keychain
service names were retained, and no value, cookie, trace, screenshot, or
browser state was stored in this repository or release evidence.

## Alias-assignment evidence

The authenticated Vercel inspection ties the two canonical aliases and the
expected project aliases to the current `READY` Production redeploy. The
retained current-deployment inspection fields did not expose a Git source, so
the release record does not pretend that direct metadata proves commit
identity. Instead, the same GitHub Production deployment event binds deployment
`5543929832` to the production commit; the operator then redeployed during the
credential rotation, and the post-rotation smoke checked out that exact commit
and observed the resulting current immutable deployment URL. This is an
explicitly labeled inference, not direct Vercel Git metadata. The retained JSON
records the limitation. It is not a DNS zone export and does not authorize the
old-domain cutover.

## Modern UI and UX evidence

The point-in-time production audit in
`docs/evidence/production-ui-audit-2026-07-22.json`, SHA-256
`28b3b2adafb81abac31e675af7dc37b9ed1e0e5fe620c2db08133193b2614c33`,
retains the reviewed desktop and mobile screenshot hashes and a Lighthouse
`13.0.3` lab run. The operator observed `100` for performance, accessibility,
best practices, and SEO, but retained no LHR, HTML report, invocation, or report
hash; those scores are therefore not independently corroborated. The full-page
captures are retained under `docs/evidence/screenshots/` and show the home
page's modernized hierarchy, responsive stacking, strategic-partnership panel,
and private-demo status without observed clipping. The separate production
audit found that `/es/terms` did clip its long hero title at `320px` and
`390px`; a tested fix exists only on the unmerged follow-up branch. This is
home-page visual evidence plus an operator-observed lab result, not whole-site
visual acceptance, field Core Web Vitals, or demo-fidelity approval.

## Release scope and boundaries

- PR #13 added a fail-closed pre-change legacy-cutover decision tool. It cannot
  issue `GO_TO_CHANGE` without the exact current release, fresh external
  evidence, nine owner approvals, successful repository commands, a private
  external receipt directory, and sufficient validity after the planned start.
- All five launch gates remain `holding`. Contact delivery remains disabled,
  legal pages remain labeled drafts outside the sitemap, and the former domain
  remains unchanged.
- Both JavaScript demo families remain private and non-indexable. The current
  credentialed operator attestation supports the executive-review path but is
  not independently reproducible evidence and does not establish rights,
  audio, strict keyframe/RMSE fidelity, or product acceptance.
- The Boulder Learning–PedaNova strategic partnership and HELP Math 1.0/2.0
  history remain published as sourced bilingual facts; no confidential module
  details were added.

## Exceptions and follow-up gates

| Gap or exception | Owner | Review date |
| --- | --- | --- |
| The authenticated private-demo counts and focused browser duration are an operator attestation; no canonical result, raw output, trace, screenshot, or session state was retained. | Engineering | Repeat with a retained credential-free canonical result for a future release if independent machine verification is required. |
| PR #13 Production clips the long `/es/terms` hero at 320px and 390px. The follow-up fix is tested but unmerged and is not covered by this release identity. | Engineering | Merge only through a separately evidenced application release. |
| A semantic smoke behind the protected PR #13 Preview was not retained; Vercel `READY` and commit identity do not prove application behavior behind SSO. | Pending release owner | Resolve for a future candidate only through the private Automation Bypass procedure. No retrospective pass is asserted. |
| Private delivery to John Ramo, his acknowledgement of the no-recording/no-forwarding boundary, and the selected post-meeting close action remain unrecorded. | Pending executive-preview owner | Complete before and immediately after the executive review; do not store the access value. |
| Durable encrypted off-device custody and an independent restore of the restricted legacy-source archive remain pending. | Pending custody owner | Complete before historical-domain cutover. |
| Legal identity, final legal copy, contact delivery, demo publication rights and strict fidelity/product acceptance, release/rollback ownership, DNS/mail cutover, Search Console, and final authorization remain pending. | Named owners not yet recorded | Required before the corresponding gate is opened. |

These exceptions prevent describing the overall website migration, historical
domain cutover, public demo publication, or final production launch as
complete. They do not alter the exact release identity, canonical alias
assignment, Quality, stable-link, zero-failure production smoke,
operator-attested current credentialed executive-preview, or scoped home-page
UI evidence retained above.
